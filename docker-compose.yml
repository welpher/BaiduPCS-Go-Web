version: '3.3'
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        - PASSWORD=YourPassword
    ports:
      - "8085:3001"
    volumes:
      - /downloads:/root/Downloads
